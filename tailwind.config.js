module.exports = {
  content: [
    "./index.html",    // Scans the main HTML file
    "./src/**/*.css"   // Scans all CSS files in the 'src' folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};